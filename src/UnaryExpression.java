/**
 * The UnaryExpression class represents a unary expression.
 */
public abstract class UnaryExpression extends BaseExpression {
    protected UnaryExpression(Expression e) {
        super(e);
    }
}
